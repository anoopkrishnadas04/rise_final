import './MarketPercentage.css'

export default function MarketPercentage({market_percentage}) {
    return (
        <div id="market-percentage-container">
            {market_percentage}
        </div>
    )
}